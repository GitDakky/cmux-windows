<Window x:Class="Cmux.Views.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update available" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" CornerRadius="0" GlassFrameThickness="0"
                      ResizeBorderThickness="0" />
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="{StaticResource WindowCornerRadius}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Color="#000000" Opacity="0.4" Direction="270" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SidebarBackgroundBrush}"
                    CornerRadius="10,10,0,0"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                        <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                   Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0" />
                        <TextBlock Text="Update available" Foreground="{StaticResource ForegroundBrush}"
                                   FontSize="12" FontWeight="SemiBold" />
                    </StackPanel>
                    <Button Style="{StaticResource WindowCloseButton}" HorizontalAlignment="Right"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Click="Later_Click" Width="38" Height="32">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="9" />
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="VersionText"
                           Grid.Row="0"
                           Foreground="{StaticResource ForegroundBrush}"
                           FontSize="16"
                           FontWeight="SemiBold" />

                <TextBlock Grid.Row="1"
                           Margin="0,6,0,10"
                           Foreground="{StaticResource ForegroundDimBrush}"
                           FontSize="12"
                           Text="A new cmux release is available on GitHub." />

                <Border Grid.Row="2" Background="#FF151515" CornerRadius="6" Padding="12"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ReleaseNotesText"
                                   Foreground="{StaticResource ForegroundBrush}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   LineHeight="18" />
                    </ScrollViewer>
                </Border>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                    <Button Content="Remind me later" Style="{StaticResource CmuxButton}"
                            Padding="12,6" Margin="0,0,8,0" Click="Later_Click" />
                    <Button Content="Update now" Style="{StaticResource AccentButton}"
                            Padding="14,6" Click="Update_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
